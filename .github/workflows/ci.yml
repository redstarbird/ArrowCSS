name: C/C++ CI Pipeline

on:
    push:
        branches: ["master"]
    pull_request:
        branches: ["master"]

jobs:
    test-platforms:
        name: ${{ matrix.name }}
        runs-on: ${{ matrix.os }}

        # Create a matrix of different OS and CMake configurations to test various scenarios
        strategy:
            fail-fast: false # Allows all matrix jobs to complete even if one job fails
            matrix:
                include:
                    - name: "Linux GCC (64-bit)"
                      os: ubuntu-latest
                      compiler: gcc
                      cmake_args: ""

                    - name: "Linux Clang with Address Sanitizer (64-bit)"
                      os: ubuntu-latest
                      compiler: clang
                      cmake_args: "-DCMAKE_C_FLAGS='-fsanitize=address,undefined -fno-omit-frame-pointer' -DCMAKE_EXE_LINKER_FLAGS='-fsanitize=address,undefined'"

                    - name: "Windows (64-bit)"
                      os: windows-latest
                      cmake_args: ""

                    - name: "macOS (64-bit)"
                      os: macos-latest
                      cmake_args: ""

        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
              with:
                  submodules: true

            # Configure CMake using the specific arguments from the matrix
            - name: Configure CMake
              env:
                  CC: ${{ matrix.compiler }}
              run: cmake -B build -S . ${{ matrix.cmake_args }}

            - name: Build
              run: cmake --build build --config Debug

            # Run CTest
            - name: Run Tests
              working-directory: ./build
              run: ctest --output-on-failure -V -C Debug
